/** @type {import('next').NextConfig} */ 
const nextConfig = {}; 
module.exports = nextConfig; 

// next.config.js
const nextConfig = {
  transpilePackages: ["leaflet"],
};
module.exports = nextConfig;